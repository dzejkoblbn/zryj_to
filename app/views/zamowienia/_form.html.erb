<%= form_for(zamowienium) do |f| %>
  <% if zamowienium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(zamowienium.errors.count, "error") %> prohibited this zamowienium from being saved:</h2>

      <ul>
      <% zamowienium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :index %>
    <%= f.text_field :index %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
