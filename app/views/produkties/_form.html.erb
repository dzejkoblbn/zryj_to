<%= form_for(produkty) do |f| %>
  <% if produkty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(produkty.errors.count, "error") %> prohibited this produkty from being saved:</h2>

      <ul>
      <% produkty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_produktu %>
    <%= f.number_field :id_produktu %>
  </div>

  <div class="field">
    <%= f.label :nazwa %>
    <%= f.text_field :nazwa %>
  </div>

  <div class="field">
    <%= f.label :cena %>
    <%= f.text_field :cena %>
  </div>

  <div class="field">
    <%= f.label :czas_przygotowania %>
    <%= f.number_field :czas_przygotowania %>
  </div>

  <div class="field">
    <%= f.label :produkty_na_zamowieniu_id %>
    <%= f.text_field :produkty_na_zamowieniu_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
