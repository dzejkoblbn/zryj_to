<p id="notice"><%= notice %></p>

<h1>Ulice</h1>

<table>
  <thead>
    <tr>
      <th>Index</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ulice.each do |ulice| %>
      <tr>
        <td><%= ulice.index %></td>
        <td><%= link_to 'Show', ulice %></td>
        <td><%= link_to 'Edit', edit_ulice_path(ulice) %></td>
        <td><%= link_to 'Destroy', ulice, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ulice', new_ulice_path %>
